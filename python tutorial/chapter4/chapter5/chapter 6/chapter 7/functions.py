#   using sum built in funtion
# a = [10,20,30]
# b = [20,45,50]
# total = sum(a+b)
# print("total",total)
#  another way we also used sum in set , tuple, list but not direct a single value
# a = (10,20,40,50)
# total = sum(a,21)  # in which we give a list name and add value which we want to add
# print(total)
#  max () function used
# num = [10,20,30,43]
# maximum = max(num)
# print("maximum number:",maximum)
#  another way by giving a single value

# minimum number
x =  int(input("enter the num"))
y =  int( input("enter the num"))

minimum = min(x,y)
print("minimum num:", minimum)
